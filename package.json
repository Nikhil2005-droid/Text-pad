{
  "name": "text-pad",
  "private": true,
  "version": "1.0.0",
  "description": "Workspace-based notes and code pad with a React frontend and Express/MongoDB backend.",
  "main": "server/server.js",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "root:install": "npm install --include=prod",
    "client:install": "npm --prefix client install --include=dev",
    "client:dev": "npm --prefix client run dev",
    "client:build": "npm --prefix client run build",
    "client:lint": "npm --prefix client run lint",
    "client:test": "npm --prefix client run test",
    "server:start": "node server/server.js",
    "build": "npm run root:install && npm run client:install && npm run client:build && node -e \"require('dotenv'); require('express'); require('mongoose'); console.log('server dependencies ready')\"",
    "start": "npm run server:start"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "google-auth-library": "^9.15.1",
    "mongoose": "^9.1.3"
  }
}
