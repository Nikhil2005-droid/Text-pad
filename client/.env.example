# Optional when the frontend is hosted separately from the backend.
VITE_API_URL=/api/workspaces
VITE_TRANSLITERATION_API_URL=/api/transliteration

# Optional local development proxy target for Vite.
VITE_DEV_API_TARGET=http://localhost:5000
